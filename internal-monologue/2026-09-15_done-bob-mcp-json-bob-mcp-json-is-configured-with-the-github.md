# 2026-09-15 — bob-mcp-json-configured-with-github

Done. `.bob/mcp.json` is configured with the GitHub MCP server.

**Next steps:**
1. Open Bob panel → Settings → MCP tab
2. See `github` appear and connect
3. On first run `npx` downloads `@modelcontextprotocol/server-github`

> `.bob/` is gitignored so the token won't be committed.
